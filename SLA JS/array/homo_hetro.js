// homogeneous values
const course = ['py', 'css', 'html', 'js', 'php', 'beta', 'sql']
for (let j = 0; j < course.length; j++) {
    console.log(course[j])
}

// hetrogeneous datatype values
const hetro = ['bb', 'gt', 16, 11, 'we', 112]
for (let k = 0; k < hetro.length; k++) {
    console.log(hetro[k])
}